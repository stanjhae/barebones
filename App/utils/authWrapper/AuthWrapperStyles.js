import { StyleSheet } from 'react-native';

const AuthWrapperStyles = StyleSheet.create({
  keyboardAvoidingViewContainer: {
    flex: 1,
    backgroundColor: 'white',
  },
  scrollViewContainer: {
    alignItems: 'center',
    flexGrow: 1,
  },
});

export default AuthWrapperStyles;
